package turtleduck.display;

public class DisplayLayout {
/*	protected int visible = 0;
	protected int focused = TERMINAL;
	protected int maximized = -1;



	protected DisplayLayout(DisplayLayout old) {
		for (int i = 0; i < sections.length; i++) {
			sections[i] = new DisplaySectionImpl(i);
		}
		visible = old.visible;
		focused = old.focused;
	}

	DisplaySection header() {
		return sections[HEADER];
	}

	DisplaySection footer() {
		return sections[FOOTER];
	}

	DisplaySection editor() {
		return sections[EDITOR];
	}

	DisplaySection navigator() {
		return sections[NAVIGATOR];
	}

	DisplaySection graphics() {
		return sections[GRAPHICS];
	}

	DisplaySection terminal() {
		return sections[TERMINAL];
	}
	void update() {
		if (Display.currentLayout == this)
			Display.update();
	}

	void use() {
		Display.currentLayout = this;
		Display.update();
	}


*/	
}
